# +required The configuration file schema version
schemaVersion: 1.1

# +optional Incoming connection details for the component
endpoints:
  # +required Unique name for the endpoint.
  - name: windows-service
    # +optional Display name for the endpoint.
    displayName: Windows Service
    # +required Service section has the user service endpoint details
    service:
      # +optional Context (base path) of the API that gets exposed via the endpoint.
      basePath: /
      # +required Numeric port value that gets exposed via the endpoint
      port: 3389
    # +required Type of traffic that the endpoint is accepting.
    # Allowed values: REST, GraphQL, WS, GRPC, TCP, UDP.
    type: TCP
    # +optional Network level visibilities of the endpoint.
    networkVisibilities:
      - Public
      - Organization

  - name: admin-interface
    # +optional Display name for the endpoint.
    displayName: Admin Interface
    # +required Service section has the user service endpoint details
    service:
      # +optional Context (base path) of the API that gets exposed via the endpoint.
      basePath: /admin
      # +required Numeric port value that gets exposed via the endpoint
      port: 8006
    # +required Type of traffic that the endpoint is accepting.
    type: REST
    # +optional Network level visibilities of the endpoint.
    networkVisibilities:
      - Public

# +optional Configuration details specific to this component
configuration:
  # Set environment variables
  environmentVariables:
    VERSION: "10"
    DISK_SIZE: "100G"
    PASSWORD: "B!0031"
    REGION: "en-US"
    KEYBOARD: "en-US"
  # Expose required ports
  exposedPorts:
    - 8006
    - 3389/tcp
    - 3389/udp

# +optional Outgoing connection details for the component.
dependencies:
  # +optional Defines the connection references from the Internal Marketplace.
  connectionReferences:
    # Example dependency, update as needed
    - name: dependency-example
      resourceRef: service:/SomeProject/SomeComponent/v1/unique-id/PUBLIC
